# 작업 중단 — 2026-05-03

## 사유

자동 tick(cron */10분) 으로 cy-003 사이클을 돌렸지만, 산출물이 **사용자가 읽고 이해할 수 없는 내부 시스템 용어 범벅**이 되어서 실패로 판단함.

조직(writer / critic / voice-keeper / orchestrator) 들이 자기들끼리 audit 용어 / 자가 참조 / "박음" / "자리" 같은 내부 메타 언어로만 회전하면서, 정작 사용자가 읽을 reader-facing 회차(2화 sideseat-dawn, 3화 paired-dawns 등)는 알아볼 수 없는 형태가 됨.

근본 원인: **시드(지형 / 인물)가 사람 손으로 충분히 박히지 않은 상태에서 자동화를 켰음.** 빈 시드 위에서 회전하면 조직들이 audit 게이트만 통과하면서 자가 증식만 한다.

## 다음번 재시작 시 순서

1. **지형부터** — 사람이 직접, 한 권역씩, 사용자가 이해할 수 있는 평범한 한국어로 빌딩.
2. **인물 하나씩** — 한 명씩, 신체적 특징 / 말투 / 일상 / 관계를 사람이 직접 박음. 자동 생성된 character sheet 들은 일단 시드 재빌딩 후에 다시 평가.
3. **시드가 충분하다고 사용자가 OK 한 뒤에** 자동 tick(cron) 을 다시 켤 것.

## 현재 상태

- cron 의 my-prompts tick 두 줄(`*/10 checker`, `5-59/10 role-rotate`)은 `# DISABLED 2026-05-03 —` 로 주석 처리됨. crontab 백업: `/tmp/crontab.backup-20260503-132110.txt`.
- 진행 중이던 tick(pid 3729045, mode=checker) 은 강제 종료됨.
- `state/tick.lock.d/` 정리됨.
- cy-003 까지의 모든 산출물은 본 커밋에 포함되어 git 히스토리에 보존됨 (재시작 시 무엇이 자가 증식이었는지 참고용).
